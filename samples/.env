# =============================================================================
# Qutora API - SQL Server Express Configuration
# =============================================================================
# NOTE: This configuration is for testing and development purposes
#
# Usage:
#   1. Copy this file to .env in the same directory
#   2. Update the values below
#   3. Run: docker-compose -f docker-compose.sqlserver.yml up -d
# =============================================================================

# -----------------------------------------------------------------------------
# APPLICATION SETTINGS
# -----------------------------------------------------------------------------
ASPNETCORE_ENVIRONMENT=Development
TZ=UTC

# -----------------------------------------------------------------------------
# SQL SERVER CONFIGURATION
# -----------------------------------------------------------------------------
# SQL Server SA password (change as needed)
# Requirements: At least 8 characters, uppercase, lowercase, numbers, and symbols
MSSQL_SA_PASSWORD=QyF77ywWfmyse5a4jVsQ

# SQL Server database name
SQLSERVER_DB=QutoraDB

# SQL Server port (default: 1433)
SQLSERVER_PORT=1433

# -----------------------------------------------------------------------------
# JWT AUTHENTICATION
# -----------------------------------------------------------------------------
# IMPORTANT: Change this secret key
# Must be at least 32 characters long
JWT_SECRET_KEY=your-very-secure-jwt-secret-key-must-be-at-least-32-characters-long-change-this
JWT_ISSUER=QutoraAPI
JWT_AUDIENCE=QutoraAPI
JWT_EXPIRY_MINUTES=60

# -----------------------------------------------------------------------------
# CORS CONFIGURATION
# -----------------------------------------------------------------------------
# Frontend application URLs
ALLOWED_ORIGIN_0=http://localhost:3000
ALLOWED_ORIGIN_1=http://localhost:5173
ALLOWED_ORIGIN_2=https://localhost:7001

# -----------------------------------------------------------------------------
# STORAGE CONFIGURATION
# -----------------------------------------------------------------------------
STORAGE_PROVIDER=filesystem
STORAGE_ROOT_PATH=/app/data
STORAGE_DEFAULT_BUCKET=default

# -----------------------------------------------------------------------------
# API PORT
# -----------------------------------------------------------------------------
API_PORT=8080

# -----------------------------------------------------------------------------
# LOGGING
# -----------------------------------------------------------------------------
LOG_LEVEL=Information

# =============================================================================
# SECURITY NOTES
# =============================================================================
# For development and testing:
# - Change MSSQL_SA_PASSWORD to a strong password
# - Change JWT_SECRET_KEY to a unique secret
# - Update CORS origins to match your frontend
#
# SQL Server Password Requirements:
# - At least 8 characters
# - Contains uppercase letters (A-Z)
# - Contains lowercase letters (a-z)
# - Contains numbers (0-9)
# - Contains special characters (!@#$%^&*)
# =============================================================================

